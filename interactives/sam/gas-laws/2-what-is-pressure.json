{
  "title": "¿Qué es la presión?",
  "publicationStatus": "public",
  "subtitle": "Explorar cómo un gas ejerce presión en ambos lados de un recipiente.",
  "about": [
    "Los gases no tienen forma ni volumen definido. Cuando están confinados a un recipiente,",
    "podemos medir la presión que ejercen en las paredes de ese recipiente.",
    "",
    "El modelo muestra la parte interior (átomos amarillos) y exterior (átomos verdes) de un globo.",
    "La barra divisoria de color verdoso representa las paredes del globo.",
    "",
    "Agregar átomos al globo. Observar lo que sucede."
  ],
  "models": [
    {
      "type": "md2d",
      "id": "page2$0",
      "url": "imports/legacy-mw-content/converted/sam-activities/gas-laws/original-interactives-in-pages/page2/page2$0.json",
      "viewOptions": {
        "controlButtons": "play_reset",
        "textBoxes": [
          {
            "text": "Adentro del globo",
            "x": 0.7,
            "y": 2.45,
            "layer": 1,
            "frame": "rounded rectangle",
            "backgroundColor": "rgb(250,250,250)"
          },
          {
            "text": "Afuera del globo",
            "x": 3.35,
            "y": 2.45,
            "layer": 1,
            "frame": "rounded rectangle",
            "backgroundColor": "rgb(250,250,250)"
          },
          {
            "text": "Pared del globo",
            "x": 2.1593974304199217,
            "y": 1.5,
            "width": 0.3,
            "textAlign": "center",
            "layer": 1,
            "hostType": "RectangularObstacle",
            "hostIndex": 0
          },
          {
            "text": "Átomos adentro: 40",
            "x": 0.7,
            "y": 0.25,
            "layer": 1,
            "frame": "rounded rectangle",
            "backgroundColor": "rgb(160,206,219)"
          }
        ]
      },
      "onLoad": [
        "onPropertyChange('atomInside', function(n) {",
        "  setTextBoxProperties(3, {text: 'Átomos adentro: ' + n}) ",
        "});"
      ]
    }
  ],
  "outputs": [
    {
      "name": "atomInside",
      "label": "Átomos adentro del globo",
      "value": [
        "var atomNum = 0;",
        "var obsX = getObstacleProperties(0).x;",
        "for (var i = 0; i < getNumberOfAtoms(); i++) {",
        "  if (getAtomProperties(i).x < obsX) {",
        "    atomNum++;",
        "  }",
        "}",
        "return atomNum;"
      ]
    }
  ],
  "components": [
    {
      "type": "checkbox",
      "id": "show-velocity",
      "text": "Mostrar velocidad",
      "property": "showVelocityVectors"
    },
    {
      "type": "button",
      "id": "add-atom",
      "text": "Agregar un átomo al globo",
      "action": [
        "var elemNum = 3;",
        "var obsX = getObstacleProperties(0).x;",
        "var atomSigma = getElementProperties(elemNum).sigma;",
        "var avgVx = 0;",
        "var avgVy = 0;",
        "var atomNum = getNumberOfAtoms();",
        "for (var i = 0; i < atomNum; i++) {",
        "  avgVx += Math.abs(getAtomProperties(i).vx);",
        "  avgVy += Math.abs(getAtomProperties(i).vy);",
        "}",
        "avgVx /= atomNum;",
        "avgVy /= atomNum;",
        "addAtom({ element: elemNum, x: (obsX - atomSigma) * Math.random()  , y: get('height') * Math.random(), vx: avgVx, vy: avgVy});"
      ]
    },
    {
      "type": "button",
      "id": "remove-atom",
      "text": "Remover un átomo de adentro del globo",
      "action": [
        "var atomNum = getNumberOfAtoms();",
        "var obsx = getObstacleProperties(0).x;",
        "if (atomNum > 0) {",
        "  for (var i = 0; i < atomNum; i++) {",
        "    if (getAtomProperties(i).x < obsx) {",
        "      removeAtom(i);",
        "      break;",
        "    }",
        "  }",
        "}"
      ]
    }
  ],
  "layout": {
    "bottom": [
      [
        "add-atom",
        "remove-atom",
        "show-velocity"
      ]
    ]
  }
}
